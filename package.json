{
  "name": "herbisight",
  "version": "1.2.1",
  "description": "Firefox extension that removes or dims AI-labeled content from YouTube and Facebook feeds.",
  "scripts": {
    "dev": "web-ext run --source-dir extension",
    "build": "web-ext build --source-dir extension --artifacts-dir dist --overwrite-dest",
    "lint": "web-ext lint --source-dir extension"
  },
  "devDependencies": {
    "web-ext": "^8.4.0"
  },
  "license": "MIT"
}
